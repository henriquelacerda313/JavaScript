/* function soma(n1, n2) {
    return n1 + n2
}

console.log(soma(2,5)) */

function soma(n1=0, n2=0) {  // colocar no parâmeto  = 0 , por que se não especificar no parâmetro valores para as duas saidas, vai retornar NaN

    return n1 + n2 
}

console.log(soma(2))